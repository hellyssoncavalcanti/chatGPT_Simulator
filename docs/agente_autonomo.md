# Agente Autônomo

Arquivo principal: `Scripts/auto_dev_agent.py`.

## Segurança operacional
- `AUTODEV_AGENT_AUTOCOMMIT` agora default `0` (dry-run por padrão).
- Fluxo recomendado: gerar patch/PR e revisar antes de merge.

## Configurações
Consulte variáveis `AUTODEV_AGENT_*` no `Scripts/config.py`.
